import { AdminRepository } from "@/data/admin-repository";
import { MetricCard } from "@/components/admin/metric-card";
export const dynamic = "force-dynamic";
export default function AnalyticsPage() { const analytics = new AdminRepository().analytics(); return <><header><p>Organization evidence</p><h1>Readiness and learning analytics</h1><p>Totals use persisted assessments, supported results, recommendations, and course completions.</p></header><section className="admin-metrics" aria-label="Organization totals"><MetricCard label="Officials" value={analytics.officials}/><MetricCard label="Completed assessments" value={analytics.completedAssessments}/><MetricCard label="Readiness" value={`${analytics.readinessPercent}%`}/><MetricCard label="Assessment coverage" value={`${analytics.assessmentCoveragePercent}%`}/><MetricCard label="Course assignments" value={analytics.courseAssignments}/><MetricCard label="Course completions" value={analytics.courseCompletions}/></section><section><h2>Supported gaps by domain</h2>{analytics.supportedGapsByDomain.length ? <ul>{analytics.supportedGapsByDomain.map((item) => <li key={item.domain}>{item.domain.replaceAll("_", " ")}: {item.gaps}</li>)}</ul> : <p>No supported competency gaps have been recorded yet.</p>}</section></>; }
